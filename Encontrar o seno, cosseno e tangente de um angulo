#Encontrar o seno, cosseno e tangente de um angulo
import math
A = int(input('Digite o angulo: '))
S = math.sin(math.radians(A))
C = math.cos(math.radians(A))
T = math.tan(math.radians(A))
print('\n O seno do angulo é {:.2f}, o Cosseno é {:.2f} e a tangente é {:.2f}'.format(S,C,T))
